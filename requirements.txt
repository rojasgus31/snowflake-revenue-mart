# Runtime dependencies for the Streamlit dashboard only.
#
# Streamlit Community Cloud installs from this file when it is present. Without
# it, the platform would resolve pyproject.toml and install the whole project --
# three dbt adapters, the Snowflake connector and the test tooling -- none of
# which the dashboard imports. The app reads the committed DuckDB file and needs
# exactly these four packages.
#
# Keep the pins consistent with pyproject.toml and uv.lock.
streamlit>=1.42,<2
duckdb~=1.5.0
pandas~=2.2.0
plotly~=5.24.0
